# 社内向け勉強会:フォームの「送信ボタンを押した後」に何が起きているか

## 概要

- **時間**: 30〜40分
- **テーマ**: フォームの送信ボタンを押した後、サーバー側で何が起きているかを、簡単なPHPコードを見ながら理解する
- **ゴール**: 「サーバーは受け取って、(裏で何かして)、返すだけ」という一番シンプルな構造を体感してもらう

> [!WARNING] 注意
> この資料はわかりやすさを優先しているため、正確さは意図的に犠牲にしている部分があります。<br>
> 気になったところがあれば、ぜひ自分で調べてみてください。

---

## 事前準備(参加者にお願いすること)

- **当日までに、PaizaCloud(paiza)のアカウントを作成しておいてください**
  - https://paiza.cloud/ にアクセスし、アカウント登録を済ませておいてください

### PaizaCloudアカウント登録手順

1. 最初の画面で「無料会員登録」をクリック

   ![PaizaCloud 会員登録](/paizacloud/paiza_0001.png)

2. アカウント作成を図の通り行う(Gmailアドレスでの作成でOK)

   ![PaizaCloud アカウント作成](/paizacloud/paiza_0002.png)

3. サーバーは「無料プラン」を選択

   ![PaizaCloud 無料プラン選択](/paizacloud/paiza_0003.png)

4. サーバー設定は図の通り選択する

   ![PaizaCloud サーバー設定](/paizacloud/paiza_0004.png)

5. 作成後、この画面が表示されればOK

   ![PaizaCloud 作成完了画面](/paizacloud/paiza_0005.png)

---

## 1. サーバーとは?

例えば、URLに https://google.com と書いてエンターを押すとどうなるでしょうか。<br>

自分のパソコンから通信されて、世界のどこかにある https://google.com という場所に置いてあるパソコンに合図が行きます。<br>
するとその置いてあるパソコンは これあげる！ と言って、HTMLやらCSSやらJavaScriptを自分のパソコンに返してくれます。<br>

それらファイルを受け取った自分のパソコンは、ブラウザを介して中身を表示します。<br>


この情報を返してくれるパソコンを「**サーバー**」といいます。<br>
一方、自分の方のパソコンは「**クライアント**」といいます。

**クライアント**から**サーバー**へ情報をちょうだい！と依頼することを**リクエスト**を送るといいます。<br>
一方、**サーバー**から**クライアント**へ情報を返すことを**レスポンス**といいます。

つまり、サーバーは受け取って返すだけのシンプルな存在です。

![リクエストとレスポンスの図解](/diagrams/request-response.svg)

> [!TIP] 🔔サーバーは特別なパソコンじゃない
> 「サーバー」と聞くと、なにか特別な機械を想像するかもしれませんが、実はそうではありません。
>
> サーバーの正体は、「リクエストを受け取ったらレスポンスを返す」というソフト(Webサーバーソフト)が入っているだけの、ただのパソコンです。<br>
> 代表的なWebサーバーソフトには`Apache`や`nginx`があります。
>
> なので、理論上は自分のMacBookも「サーバー」にできます。

ここでもう一つ、Webサイトには「静的サイト」と「動的サイト」という分け方があります。

- **静的サイト**: あらかじめ用意されたHTMLファイルを、そのままレスポンスとして返すサイト。誰がアクセスしても同じ内容が返ります。
- **動的サイト**: アクセスしてきたユーザーや入力内容によって、レスポンスの内容を変えるサイト。「ログインしている人には名前を表示する」「フォームの入力内容に応じて表示を変える」といったことができます。

<!-- ![静的サイトと動的サイトの違い](/diagrams/static-vs-dynamic.svg) -->

この動的なコンテンツをユーザーごとに組み立てて返すために使われるプログラムの一つが、**PHP**です。<br>
サーバー上でPHPが動くことで、アクセスしてきた人や送られてきたデータに応じて、レスポンス(HTML)の内容を変えることができます。

> [!TIP] 🔔PHPはTEXTとして送られる
> 実はPHPファイルはサーバー側で処理されると、最終的に、TEXTとなってブラウザにレスポンスされます。
> (さらに実は、HTMLもテキストとしてブラウザに渡されます。)
> PHPファイルにHTMLタグを(ちゃんとした方法で)書くと、それがブラウザに戻るときにはTEXTとして渡されます。
> ブラウザは、TEXTの中にHTMLタグがあるとそれを処理して描画します。
> つまりPHPにはHTMLタグもかけるよ、ということです。詳細はハンズオンで確認しましょう。

#### 振り返り

- 静的サイトの具体例、動的サイトの具体例をそれぞれ考えてみてください。
- クライアント・サーバーの関係性を説明してください。

> [!TIP] 🔔仮想化されたサーバー
> サーバーパソコンを用意して運用することをオンプレミスといいます。
> そのパソコンを用意したり、そのパソコンを置く場所を用意したり、そのパソコンの管理をしたり、不要になったら処分したりと管理が面倒です。
> 世の中の開発者は「めんどいから誰か代わりにやってくれないかな」と考えました。
> 
> そこでWEB上にて、仮想的にサーバーを用意して誰もが自由に借りられるようなサービスを提供する会社が現れました。
> これが、AWSや、Google Cloud、Microsoft Azureです。
> 面倒な管理等は彼らが行ってくれて、不要になったら契約を切ればそれで終了できます。

## 2. フォームからサーバーに送る

これから実際にやることを、リクエスト/レスポンスの流れで見てみましょう。

![ハンズオンの流れ(ページを開く→フォーム送信)](/diagrams/handson-flow.svg)

では、実際にPaizaCloud上で作ってみましょう。

1. PaizaCloudにログインし、新規サーバーを作成する(これは事前準備ができていればok)
2. サーバー起動後、`public_html`フォルダに`index.html`を新規作成し、↓のコードをコピペ・保存する。

![PaizaCloud 新規ファイル作成](/paizacloud/paiza_0006.png)

`index.html`:

```html
<form action="contact.php" method="POST">
    <p>お名前: <input type="text" name="name"></p>
    <p>メールアドレス: <input type="email" name="email"></p>
    <button type="submit">送信</button>
</form>
```

`action`は送り先のファイル、`method`は送り方(ここでは`POST`)を指定しています。

`name`属性は、サーバー側で受け取るときの「名札」になります。

> [!TIP] 🔔methodについて
> ここでは、POSTというものを書いていますが、もう一つGETというものがあります。
> GETとPOSTは明確に異なり、用法も異なります。
> 一旦ここでは説明を省きますので、気になる方はAIか福島さんに聞いてください。


> [!TIP] 🔔actionについて
> ここに書いた先に、リクエストを送ることができます。
> 今回の場合は、同じサーバー内に送りたいので、このような書き方になっています。
> 例えば、受け取る準備ができていれば、https://HOGEFUGA.jp といった任意の場所に送ることが可能です。


## 3. PHP側で受け取る

では次に、`contact.php`も作成しましょう。
このファイルでは、リクエストされて受け取った内容をphpで動的に変化させてレスポンスします。

1. `public_html`フォルダに`contact.php`を新規作成する
2. 下記のコードをコピペする


```php
<?php
// フォームから送られてきたデータを受け取る
$name = $_POST['name'];
$email = $_POST['email'];


// 本来はここで入力内容をチェックする(バリデーション)処理が入る

// 本来はここでデータベースに保存する処理が入る
// 例: お問い合わせ内容を保存する、会員情報を更新する など

// 受け取った内容をブラウザに返す
echo "<h1>お問い合わせありがとうございます、" . $name . "さん</h1>";

// print_rで配列の中身を確認できる、ということを確認するため、適当な配列を用意
$array = [$name, $email];
print_r($array);


```

ここで持ち帰ってほしいポイントは、この3つです。

- `$_POST['name']`という特別な書き方で、フォームの`name="name"`で送られてきた中身を取得できます(同様に`$_POST['email']`で`name="email"`の中身が取れます)
- サーバーは「受け取って」「(裏で何かして)」「返す」の3ステップだけです
- `echo`で書いたものが、そのままブラウザの画面に表示されます。ただし、`echo`の中にHTMLタグ(`<h1>`など)を書くと、その文字列がブラウザに渡り、ブラウザはHTMLタグとして解釈して見た目を組み立てます。

## 4. 実際に確認してみる

1. 「ブラウザ」アイコンから`https://<サーバ名>.paiza-user-free.cloud/~ubuntu/index.html`にアクセスする
2. フォームに入力して送信し、`contact.php`の結果が表示されることを確認する

サーバー名を忘れた方は、ここから見られます。
   ![PaizaCloud サーバー名](/paizacloud/paiza_0007.png)

   ![PaizaCloud サーバー名２](/paizacloud/paiza_0008.png)

#### 振り返り

- 自分が今やった「入力→送信→表示」の一連の流れを、サーバーの3ステップ(受け取って/裏で何かして/返す)に当てはめて30秒説明してみてください

## 5. 質疑(残り時間)

---

## 使用環境:PaizaCloud

- https://paiza.cloud/
- ブラウザだけでWebサーバー(Apache)+PHPが動く環境が用意できる
- 各MacBookへのPHPインストールやDocker環境構築が不要
- `新規サーバ作成`→用意されたテンプレートから`public_html`配下にファイルを作成し、`ブラウザ`アイコンから動作確認ができる
- アクセスURLの例: `https://<サーバ名>.paiza-user-free.cloud/~ubuntu/index.php`のような形式でアクセスする(サブディレクトリ`~ubuntu`が必要な点に注意)
- 無料プランには利用時間の制限があるため、当日は開始前にサーバーを起動しておく

> [!NOTE] 無料プランの制限について
> - **同時起動数**: サーバーは同時に1台まで
> - **利用期間**: 1つのサーバー環境は作成から24時間で使えなくなる
> - **連続起動時間**: ブラウザを開いている間のみ稼働し、最大4時間(タブを閉じると停止する)
> - **スペック**: CPU 2コア / メモリ2GB / ディスク1GB
> - **外部公開**: 独自ドメインでの一般公開は不可。ただし`https://<サーバ名>.paiza-user-free.cloud/...`のようなURLへの一時的なアクセスは可能(このハンズオンで使う分には問題なし)
>
> **参加者への案内**: ハンズオン中はPaizaCloudのタブを閉じない(閉じるとサーバーが停止する)。万一サーバーが使えなくなった場合は、新規サーバーを作成し直せばリカバリできる